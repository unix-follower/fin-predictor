apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fin-predictor.fullname" . }}-test-connection"
  labels:
    {{- include "fin-predictor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fin-predictor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
